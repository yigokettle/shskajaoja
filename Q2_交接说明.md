# D 题 问题2 交接说明（N=23 最小化 Cmax）

> 交接目的：把已验证的建模口径、已走通/已走死的算法路径、以及下一步该怎么做，
> 完整交给另一个 AI 继续跑。**重点是不要重复我已经证伪的三条弯路。**

工作目录：`/Users/shaw/Downloads/赛题/赛题/D题`
Python 环境：`.venv/bin/python`（已装 ortools、pandas、numpy、rasterio）

---

## 0. 当前战绩（一句话）

| 指标 | 值 | 状态 |
|---|---|---|
| 架次数 N | 23 | 固定 |
| **Cmax** | **6568.168 s** | ✅ 16 项约束独立验证通过 |
| 总能耗 | 71.3885 kWh | |
| 按期率 | 80/80（加权延误 0） | 严格时效 |
| 机型配比 | A=10 / B=7 / C=6 | |
| 带证明的下界 LB | 5942 s | 面积松弛 OPTIMAL |
| **剩余缺口** | **626 s** | ← 下一步要吃掉的就是这个 |

解文件：`Q2_架次优化/solution_bd23.json`
排班表：`Q2_架次优化/bd23_trip_schedule.csv`、`bd23_box_delivery.csv`

**用户诉求：别人有 23 架次 Cmax<6000s，我们要打到 6000s 以下。**
目前 6568s 还差 568s。下界 5942s<6000s，所以 **<6000s 未被证伪，理论上仍可能**。

---

## 1. 问题口径（这部分已校准完毕，不要再改）

### 1.1 资源
```
机型   数量  载重kg  体积m3  可用电量kWh  满充时间s
A       4      25    0.060     4.5×0.8      1800
B       2      30    0.073     4.0×0.8      2400
C       2      80    0.250     8.0×0.8      3000
电池组  A:6  B:4  C:4    （电池与机型绑定，同机型内共享）
```
放电深度 RHO=0.20，即单架次能耗上限 = (1-0.20)×EUSE[机型]。

### 1.2 两类资源占用（关键，容易写错）
- **无人机**占用 `[start, start+duration)`
- **电池**占用 `[start, start+duration+charge)` ← 含充电，比飞机久得多

C 型满充 3000s，所以 C 型电池是最容易成为瓶颈的资源。

### 1.3 时效性
- 严格模式（strict，本题采用）：每箱交付时刻 ≤ **期望送达时刻**
- 硬截止：31 箱有硬截止，必须 ≤ 硬截止时刻
- 交付时刻 = `start + delivery_offsets[箱号]`

### 1.4 物理模型（已 0 误差复现原池，**千万不要再动**）
这三个常数是我反解原池 572 个候选逐个对齐出来的，改任何一个都会偏：
```python
R_EARTH   = 6378137.0   # WGS84 长半轴，不是 6371000！（反解 300 条腿，std<1e-6）
G         = 9.81        # 不是 9.8（差 1.00102 倍，会让爬升能耗全偏）
PEAK_STEP = 1.0         # DEM 峰值采样步长，必须 ≤3m（15m 会让 cruise_alt 偏 10.14m）
```
自检结果：全 572 候选 **时长误差 0.00000000s、能耗误差 0.0000000000 kWh、腿字段最大差 1.1e-9** ✅
脚本：`scripts/q2_gen_pool.py`（含 `--selftest`）

---

## 2. 核心洞察（这是我花最多时间换来的，直接用）

### 2.1 真瓶颈是 C 型**电池**，不是飞机队列
在旧解 bs23（Cmax=6875s）上做负载分解：
```
机型  架次  飞行/机队        (飞行+充电)/电池
A     10    20823/4=5206     32534/6=5422
B      6    12027/2=6014     22749/4=5687
C      7    13328/2=6664     27195/4=6799  ← 真瓶颈（电池 > 机队）
全局完美配平下界 46179/8 = 5772 s
```
C 型 7 架次时，电池负载 6799s 直接顶住 Cmax。

### 2.2 破局点：把 C 型压到 6 架次
面积松弛给出的最优配比是 **A=10/B=7/C=6**，三机型负载被压平到 5794/5916/5940s。
只把 1 个架次从 C 挪到 B，效果就是决定性的。
**bd23（6568s）正是这个配比。** 这条路是对的，继续沿着走。

### 2.3 面积松弛下界（可写进论文的不可达性工具）
松弛掉排程，只保留任何可行解都必须满足的必要条件：
```
机队: Σ_{机型g选中} duration_i        ≤ K_g × Cmax
电池: Σ_{机型g选中} (dur+charge)_i    ≤ B_g × Cmax
```
因 [0,Cmax] 内 K_g 台机最多提供 K_g×Cmax 机时，这是**必要条件**，故松弛最优值
是真实 Cmax 的**严格下界**。没有 start 变量、没有 Cumulative，万级池秒级出解。

实测（`scripts/q2_lb_relax.py`，池 pool_c8.json 11936 个）：
```
N=23 固定 : 松弛最优 5942s，证明下界 5942s (OPTIMAL)，配比 A=10/B=7/C=6
N   自由 : 松弛最优 5738s，证明下界 5604s，          配比 A=14/B=7/C=5
```
> ⚠️ 重要更正：5942s 只是**下界**，不代表可达。我曾误导性地暗示它接近可达，
> 后被 22 轮排程 INFEASIBLE 否证。「面积够」和「时序排得开」是两件事。

---

## 3. ⛔ 三条已证伪的弯路（**不要重复**）

### 弯路1：直接在大池上跑完整 CP-SAT（选架次+排程一体）
现象：2329 / 5797 / 11936 / 21547 四种池规模，**全部 UNKNOWN**，连首个可行解都判不出。
甚至 900s 预算也不行。原因：`AddCumulative` + 数千个 optional interval 把传播搞爆。
结论：**必须分解，或必须把池压到原池量级（<1000）并配暖启动。**

### 弯路2：给 C 型找「低能耗」架次以加快电池周转（pool_bat 路线）
思路看着很对（能耗低→充电快→电池周转快），我专门写了 `q2_pool_bat.py` 构了
5797 候选池（C 型充电 683~2534s，比原来 1416+ 明显更快）。
结果：**只降到 6857s（比 6875s 仅降 18s），且 6287s 处 INFEASIBLE**，证明该池候选集太窄。
结论：**方向错了。** 正解不是让 C 型飞得更省，而是让 C 型**少飞一个架次**（见 2.2）。

### 弯路3：Benders 里用 no-good cut
`scripts/q2_benders.py` 现在用的就是这个，能跑通但**效率极低**：
每轮只排除**一个** 23 元组合，而组合数是天文数字。
实测 22 轮全 INFEASIBLE，LB 才从 5806 爬到 5942s，第 23 轮靠运气撞出 6569s，
第 24 轮主问题 UNKNOWN 就退出了（没收敛，缺口还剩 627s）。
结论：**cut 太弱，必须强化**（见第 5 节）。

---

## 4. 可用资产

### 4.1 候选池
| 文件 | 规模 | 说明 |
|---|---|---|
| `Q2_开源对比与优化/optimization_pool.json` | 572 | **原池**，物理模型基准 |
| `Q2_架次优化/pool_c8.json` | 11936 | 主力池，bd23 就是从这出的，**推荐用这个** |
| `Q2_架次优化/pool_aug.json` | 797 | 原池+A/B短程，小池+暖启动可行 |
| `Q2_架次优化/pool_bat.json` | 5797 | 电池导向，**已证伪，别用** |
| `Q2_架次优化/pool_expanded.json` | 3.75M | 全量扩充，时长范围仍 1070~3201s（说明池大小不是瓶颈，节点几何才是） |

候选记录字段：
```json
{"model":"A","route":["S07"],"boxes":["B012"],"mass_kg":..,"volume_m3":..,
 "duration_s":..,"charge_s":..,"energy_kwh":..,"delivery_offsets":{"B012":1234.5}}
```

### 4.2 脚本
| 脚本 | 用途 | 状态 |
|---|---|---|
| `scripts/q2_gen_pool.py` | 造池，物理模型唯一真源 | ✅ 0 误差 |
| `scripts/q2_lb_relax.py` | **面积松弛下界**，秒级 | ✅ 好用 |
| `scripts/q2_benders.py` | Benders 分解 | ⚠️ cut 太弱，待强化 |
| `scripts/q2_cmax_bs.py` | 二分可行性+暖启动 | 小池可用，大池 UNKNOWN |
| `scripts/q2_verify2.py` | **独立验证 16 项约束** | ✅ 必须跑 |
| `scripts/q2_pool_prune.py` / `q2_pool_slim.py` / `q2_pool_aug.py` | 剪枝构池 | 可用 |
| `scripts/q2_pool_bat.py` | 电池导向构池 | 已证伪 |

### 4.3 验证命令（**任何新解都必须过这个**）
```bash
MPLCONFIGDIR=/tmp/mplcache .venv/bin/python scripts/q2_verify2.py --tag bd23
```
检查 16 项：箱覆盖80/唯一80、期望延误、硬截止、载重、体积、能量上限、
无人机不重叠、电池不重叠、机队上限、电池上限、需求覆盖53项 等。

---

## 5. ✅ 下一步怎么打到 <6000s（给接手 AI 的具体方案）

现状：UB=6568s，LB=5942s，缺口 626s。目标 <6000s。

### 方案A（首选）：强化 Benders 的割
改 `scripts/q2_benders.py`，两处动刀：

1. **机型计数割**替代 no-good cut。
   现在一轮只砍一个组合，太慢。既然 A=10/B=7/C=6 下连续十几个组合 INFEASIBLE，
   应该把割打在**配比层面**，一次排除一类。或对 INFEASIBLE 的子集提取
   「冲突核」（哪几个架次互斥），只砍这个核，泛化能力强得多。

2. **让子问题回灌真实下界**。
   现在子问题只返回 INFEASIBLE/可行，信息全丢。应改成：在放宽的 Cmax 上求
   **最小可行 Cmax(S)**，把这个值回灌进 LB。这样 [LB,UB] 才会真正收缩。

判定标准：若强化后 LB 越过 6000s → **等于证明 N=23 下 <6000s 不可达**，
这本身就是能写进论文的强结论；若 LB 仍低而 UB 降不下来，说明要换建模。

### 方案B：大邻域搜索（LNS）
以 bd23（6568s）为起点，每次随机「拆掉」3~5 个架次的箱子，
在池里重新选+重排这部分（子问题只有几个区间，秒级），接受改善解。
这是工程上最容易吃掉 500s 缺口的办法，且不需要证明最优。
起点用 `solution_bd23.json`，池用 `pool_c8.json`。

### 方案C：检查别人 <6000s 的口径是否一致
我无法用自己的模型复现 <6000s。**建议核对对方是否**：
- 也用严格时效（80/80 按期，而非只满足硬截止）
- 也算电池充电占用（`[start, dur+charge)` 而非只 `[start,dur)`）
- 电池数是否也是 A6/B4/C4
- 是否也是 R=6378137 / g=9.81 的物理口径

若对方放宽了任一条，6000s 以下就不奇怪了。**口径不同的数字不可直接比较。**

---

## 6. 历史解一览（Pareto 备查）

| tag | N | Cmax(s) | 能耗(kWh) | 说明 |
|---|---|---|---|---|
| **bd23** | 23 | **6568** | 71.3885 | ✅ **当前最优**，Benders |
| bat23 | 23 | 6857 | — | 电池导向池，已废 |
| bs23 | 23 | 6875 | 71.3845 | 二分+暖启动 |
| cN25 | 25 | 6861 | 70.485 | |
| lexN23 | 23 | 6991 | 69.751 | 字典序基线 |
| tN23 | 23 | 6991 | 72.166 | |
| lexN22 | 22 | 7376 | 72.504 | |
| lexN21 | 21 | 8226 | 66.785 | |
| c8N23 | 23 | 8677 | 64.201 | |
| lexN20 | 20 | 8777 | 67.416 | |
| eN23 | 23 | 10263 | 63.113 | 能耗优先 |

---

## 7. 复现 bd23 的命令

```bash
cd /Users/shaw/Downloads/赛题/赛题/D题

# 1. 算面积松弛下界（秒级，先确认目标可达性）
MPLCONFIGDIR=/tmp/mplcache .venv/bin/python scripts/q2_lb_relax.py \
    --n 23 --pool Q2_架次优化/pool_c8.json --seconds 300

# 2. Benders 求解（约 12 分钟，注意：不要用 grep 管道，会吞掉输出！）
mkdir -p logs
nohup env MPLCONFIGDIR=/tmp/mplcache PYTHONUNBUFFERED=1 .venv/bin/python \
    scripts/q2_benders.py --n 23 --pool Q2_架次优化/pool_c8.json \
    --iters 40 --master-seconds 30 --sub-seconds 30 --ub 6857 --gap 30 \
    --tag bd23 > logs/bd23.log 2>&1 &

# 3. 验证（必做）
MPLCONFIGDIR=/tmp/mplcache .venv/bin/python scripts/q2_verify2.py --tag bd23
```

> 踩坑提醒：`... | grep -vi findfont` 这种管道会把 Python 输出全缓冲，
> 导致日志 0 字节、进程跑 77 分钟啥也看不到。**一律直接重定向到文件 + PYTHONUNBUFFERED=1。**
